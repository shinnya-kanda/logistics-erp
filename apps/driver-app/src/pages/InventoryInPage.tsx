import { PageShell } from "./PageShell.js";

export function InventoryInPage() {
  return (
    <PageShell>
      <section className="scanner-shell" aria-label="入庫">
        <div className="scanner-panel">
          <h2 className="scanner-title">未実装：入庫</h2>
          <p className="scanner-sub">
            入庫専用コンポーネントが未分離のため、B7-24では仮ページとして表示しています。
          </p>
        </div>
      </section>
    </PageShell>
  );
}
